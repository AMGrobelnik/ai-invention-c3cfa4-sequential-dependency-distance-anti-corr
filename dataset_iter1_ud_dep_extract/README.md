# UD Dep Extract

`demo/` — Self-contained demo (Colab-ready notebook or markdown). Run without setup.  
`src/` — Full source code, data, and outputs from the experiment execution.

**Type:** dataset  
**ID:** `data_id3_it1__opus`

## Summary

Extracted dependency structures from 233 qualifying Universal Dependencies treebanks (commul/universal_dependencies, UD v2.17) via HuggingFace. Contains 976,187 sentences with >=15 tokens across 233 treebanks (from 336 processed). Each example provides head_array (int), deprel_array (string), computed dependency distance sequence (DD_i = |position_i - head_i|), token count, and length bucket (ge15/ge20). Treebank-level metadata includes Case morphological feature proportions, spoken/written modality flags (16 spoken treebanks), genre, and sentence counts at both thresholds. Treebank qualification: >=50 sentences at 20-token threshold. Output split into 16 parts under 45MB each (568MB total). Conforms to exp_sel_data_out schema.

## Output Files

- `data.py`
- `full_data_out.json`
- `preview_data_out.json`
- `mini_data_out.json`

## Demo Files

- **data.py** — Dataset fetching/generation script

---
*Generated by AI Inventor Pipeline*
